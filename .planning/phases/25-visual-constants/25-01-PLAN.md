---
phase: 25-visual-constants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/constants/visual_constants.dart
  - lib/shared/widgets/resource_badge.dart
  - test/constants/visual_constants_test.dart
  - test/widgets/resource_badge_test.dart
  - lib/features/city/screens/city_screen.dart
  - lib/features/map/screens/city_grid_screen.dart
  - lib/features/city/screens/building_upgrade_sheet.dart
  - lib/features/trade/screens/trade_dialog.dart
  - lib/features/battles/screens/widgets/pillage_result_card.dart
  - lib/features/espionage/screens/spy_report_dialog.dart
autonomous: true
requirements: [ICON-01, ICON-02, ICON-03]

must_haves:
  truths:
    - "All 5 production resources show colored circle + letter badge (W, M, C, S, G) in resource bar"
    - "Wine keeps Icons.wine_bar + purple color — not part of circle+letter system"
    - "Every screen displaying resources uses the same icon/color from visual_constants.dart"
    - "Every screen displaying buildings uses per-type icon/color from visual_constants.dart instead of binary isProduction logic"
    - "No file contains a private _icon() or _color() switch on ResourceType"
    - "No file contains isProductionBuilding ? Icons.factory : Icons.home"
  artifacts:
    - path: "lib/core/constants/visual_constants.dart"
      provides: "resourceTypeColor, resourceTypeLetter, resourceTypeIcon, buildingTypeColor, buildingTypeIcon maps"
      contains: "resourceTypeColor"
    - path: "lib/shared/widgets/resource_badge.dart"
      provides: "ResourceBadge widget — CircleAvatar + letter for production resources, Icon for wine"
      min_lines: 20
    - path: "test/constants/visual_constants_test.dart"
      provides: "Map completeness tests for all visual constant maps"
    - path: "test/widgets/resource_badge_test.dart"
      provides: "Widget render test for ResourceBadge"
  key_links:
    - from: "lib/features/city/screens/city_screen.dart"
      to: "lib/core/constants/visual_constants.dart"
      via: "import and map lookup"
      pattern: "resourceTypeColor\\[|buildingTypeIcon\\["
    - from: "lib/features/map/screens/city_grid_screen.dart"
      to: "lib/core/constants/visual_constants.dart"
      via: "import and map lookup"
      pattern: "buildingTypeIcon\\[|buildingTypeColor\\["
    - from: "lib/shared/widgets/resource_badge.dart"
      to: "lib/core/constants/visual_constants.dart"
      via: "import resourceTypeColor and resourceTypeLetter"
      pattern: "resourceTypeColor|resourceTypeLetter"
---

<objective>
Define canonical resource and building icon/color constants in a single source of truth, create a shared ResourceBadge widget, and replace all ad-hoc icon/color switch methods across 6 UI files.

Purpose: Eliminate 5 duplicate resource icon/color switch statements and 3 binary building icon conditionals, ensuring every screen uses the same visual representation.
Output: visual_constants.dart, resource_badge.dart, updated 6 consumer files, 2 test files.
</objective>

<execution_context>
@C:/Users/Bilal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bilal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-visual-constants/25-RESEARCH.md

@lib/core/constants/unit_constants.dart
@lib/core/constants/resource_constants.dart
@lib/core/constants/building_constants.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create visual_constants.dart, ResourceBadge widget, and tests</name>
  <files>
    lib/core/constants/visual_constants.dart,
    lib/shared/widgets/resource_badge.dart,
    test/constants/visual_constants_test.dart,
    test/widgets/resource_badge_test.dart
  </files>
  <behavior>
    - resourceTypeColor map has entries for all 6 ResourceType values (wood, marble, crystal, sulfur, gold, wine)
    - resourceTypeLetter map has entries for all 6 ResourceType values (W, M, C, S, G, V)
    - resourceTypeIcon map has entries for all 6 ResourceType values
    - buildingTypeColor map has entries for all 14 BuildingType values
    - buildingTypeIcon map has entries for all 14 BuildingType values
    - ResourceBadge renders a CircleAvatar with letter text for production resources (wood, marble, crystal, sulfur, gold)
    - ResourceBadge renders Icons.wine_bar icon (not circle+letter) for wine per user decision
    - ResourceBadge backgroundColor matches resourceTypeColor for the given type
  </behavior>
  <action>
    1. Create `lib/core/constants/visual_constants.dart` following the exact pattern from `unit_constants.dart` (lines 219-255). Import ResourceType from `resource_constants.dart` and BuildingType from `building_constants.dart`. Define these const maps:

    - `resourceTypeColor`: Map<ResourceType, Color> — wood: 0xFF388E3C (green.shade700), marble: 0xFF757575 (grey.shade600), crystal: 0xFF42A5F5 (blue.shade400), sulfur: 0xFFEF6C00 (orange.shade700), gold: 0xFFFFA000 (amber.shade700), wine: 0xFF8E24AA (purple.shade600 per locked decision in PROJECT.md)
    - `resourceTypeLetter`: Map<ResourceType, String> — W, M, C, S, G, V (wine='V' to avoid clash with Wood's 'W')
    - `resourceTypeIcon`: Map<ResourceType, IconData> — wood: Icons.forest, marble: Icons.square, crystal: Icons.diamond, sulfur: Icons.local_fire_department, gold: Icons.monetization_on, wine: Icons.wine_bar
    - `buildingTypeColor`: Map<BuildingType, Color> — all 14 BuildingType values with distinct colors (see research Architecture Pattern 2 for exact hex values)
    - `buildingTypeIcon`: Map<BuildingType, IconData> — all 14 BuildingType values with distinct Material icons (see research Architecture Pattern 2 for exact icon choices)

    2. Create `lib/shared/widgets/resource_badge.dart` — a StatelessWidget with:
    - Constructor: `ResourceBadge({required ResourceType type, double radius = 10.0})`
    - For wine (type == ResourceType.wine): render `Icon(Icons.wine_bar, size: radius * 1.4, color: resourceTypeColor[type])` — NOT circle+letter per user decision
    - For all other types: render `CircleAvatar(radius: radius, backgroundColor: resourceTypeColor[type], child: Text(letter, style: white bold, fontSize: radius * 0.9))`

    3. Create `test/constants/visual_constants_test.dart`:
    - Test that resourceTypeColor.length == ResourceType.values.length
    - Test that resourceTypeLetter.length == ResourceType.values.length
    - Test that resourceTypeIcon.length == ResourceType.values.length
    - Test that buildingTypeColor.length == BuildingType.values.length
    - Test that buildingTypeIcon.length == BuildingType.values.length
    - Test specific values: resourceTypeColor[ResourceType.wine] is purple.shade600, resourceTypeLetter[ResourceType.wood] == 'W'

    4. Create `test/widgets/resource_badge_test.dart`:
    - Test that ResourceBadge(type: ResourceType.wood) renders a CircleAvatar containing 'W'
    - Test that ResourceBadge(type: ResourceType.wine) renders an Icon (Icons.wine_bar), not a CircleAvatar with letter
  </action>
  <verify>
    <automated>cd /c/Users/Bilal/Desktop/Projects/ikariam && flutter test test/constants/visual_constants_test.dart test/widgets/resource_badge_test.dart</automated>
  </verify>
  <done>visual_constants.dart exports 5 complete maps covering all enum values. ResourceBadge renders circle+letter for 5 production resources and wine_bar icon for wine. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all ad-hoc resource and building icon/color usage across 6 files</name>
  <files>
    lib/features/city/screens/city_screen.dart,
    lib/features/map/screens/city_grid_screen.dart,
    lib/features/city/screens/building_upgrade_sheet.dart,
    lib/features/trade/screens/trade_dialog.dart,
    lib/features/battles/screens/widgets/pillage_result_card.dart,
    lib/features/espionage/screens/spy_report_dialog.dart
  </files>
  <action>
    For each file below, add `import '../../../core/constants/visual_constants.dart';` (adjust relative path as needed) and `import '../../../shared/widgets/resource_badge.dart';`. Then make the specific replacements:

    **A. city_screen.dart** (~lines 505-536, 647-659):
    - Delete the private `_ResourceChip` class entirely (it has `_icon()` and `_color()` switch methods)
    - Replace all `_ResourceChip(type: ...)` usages with `ResourceBadge(type: ...)` followed by the amount text
    - Delete `_ProductionBreakdownSheet._resourceIcon()` method
    - Replace its usages with `ResourceBadge(type: type, radius: 10)` or `Icon(resourceTypeIcon[type]!, ...)`

    **B. city_grid_screen.dart** (~lines 307, 463-512):
    - Delete the private `_ResourceChip` class (local copy, noted as duplicate at line 372)
    - Replace all `_ResourceChip(type: ...)` usages with `ResourceBadge(type: ...)`
    - In `BuildingCell.build()`: replace `isProduction ? Icons.factory : Icons.home` with `buildingTypeIcon[building.buildingType] ?? Icons.home`
    - In `BuildingCell.build()`: replace `isProduction ? theme.colorScheme.tertiary : theme.colorScheme.primary` color logic with `buildingTypeColor[building.buildingType] ?? theme.colorScheme.primary`

    **C. building_upgrade_sheet.dart** (~lines 168, 358-373):
    - Delete `_BuildingUpgradeContentState._resourceIcon()` method
    - Replace header icon `isProductionBuilding ? Icons.factory : Icons.home` with `buildingTypeIcon[building.buildingType] ?? Icons.home`
    - Replace resource icon usages with `ResourceBadge(type: type, radius: 10)`

    **D. trade_dialog.dart** (~lines 307-313):
    - Delete the top-level `_resourceIcon(String resourceName)` function
    - Where it was used: convert string key to enum via `resourceTypeFromDbName(key)` (already imported from building_constants.dart), then use `ResourceBadge(type: type)` widget
    - For any remaining icon-only contexts, use `resourceTypeIcon[type]`

    **E. pillage_result_card.dart** (~lines 163-191):
    - Delete `_ResourceRow._iconForResource()` and `_ResourceRow._colorForResource()` methods
    - These use string keys ('wood', 'marble', etc.) — convert with `resourceTypeFromDbName(key)` then use `ResourceBadge(type: type, radius: 10)`
    - Guard with try/catch: if conversion fails, fall back to `Icon(Icons.help_outline)`

    **F. spy_report_dialog.dart** (~lines 290-330):
    - Delete `_resourceIcon()`, `_resourceColor()`, and `_buildingIcon()` methods
    - Replace resource displays: convert string key with `resourceTypeFromDbName()`, use `ResourceBadge(type: type)`
    - Replace building icon: use `buildingTypeIcon[buildingType] ?? Icons.home`

    **IMPORTANT:** After all replacements, verify no file still contains:
    - A private `_icon(ResourceType` or `_color(ResourceType` method
    - `isProductionBuilding ? Icons.factory` or `isProduction ? Icons.factory`
    - Any `switch (type) { case ResourceType.wood: return Icons.forest` pattern
  </action>
  <verify>
    <automated>cd /c/Users/Bilal/Desktop/Projects/ikariam && flutter analyze --no-fatal-infos lib/features/city/screens/city_screen.dart lib/features/map/screens/city_grid_screen.dart lib/features/city/screens/building_upgrade_sheet.dart lib/features/trade/screens/trade_dialog.dart lib/features/battles/screens/widgets/pillage_result_card.dart lib/features/espionage/screens/spy_report_dialog.dart && flutter test test/constants/visual_constants_test.dart test/widgets/resource_badge_test.dart</automated>
  </verify>
  <done>All 6 files import from visual_constants.dart. No private _icon/_color switch methods remain. No binary isProduction building icon logic remains. ResourceBadge used for resource display. All analyze clean, all tests pass.</done>
</task>

</tasks>

<verification>
1. `flutter analyze --no-fatal-infos` passes for all modified files
2. `flutter test test/constants/visual_constants_test.dart test/widgets/resource_badge_test.dart` passes
3. `grep -rn "_icon(ResourceType\|_color(ResourceType\|isProductionBuilding ? Icons.factory\|isProduction ? Icons.factory" lib/` returns no matches
4. `grep -rn "resourceTypeColor\|buildingTypeIcon\|ResourceBadge" lib/features/` shows imports in all 6 consumer files
</verification>

<success_criteria>
- visual_constants.dart defines 5 maps covering all ResourceType (6) and BuildingType (14) enum values
- ResourceBadge renders circle+letter for 5 production resources, wine_bar icon for wine
- All 6 consumer files use shared constants — zero private icon/color switch methods remain
- All 6 consumer files use ResourceBadge for resource display
- All 3 building icon locations use buildingTypeIcon map instead of binary conditional
- flutter analyze clean, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/25-visual-constants/25-01-SUMMARY.md`
</output>
