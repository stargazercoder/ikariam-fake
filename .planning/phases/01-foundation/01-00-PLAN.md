---
phase: 01-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/unit/profile_validation_test.dart
  - test/unit/auth_persistence_test.dart
  - integration_test/auth_test.dart
  - integration_test/city_placement_test.dart
  - integration_test/rls_test.dart
  - test/helpers/mocks.dart
  - test/helpers/test_helpers.dart
  - pubspec.yaml
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - INFR-02
  - INFR-03

must_haves:
  truths:
    - "flutter test runs and exits with zero test failures (scaffolds pass trivially or are pending)"
    - "Test directory structure exists with helpers, unit, and integration test directories"
    - "mocktail dependency is available for mocking Supabase client in later tests"
  artifacts:
    - path: "test/unit/profile_validation_test.dart"
      provides: "Scaffold for AUTH-03 display_name validation tests"
      contains: "profile"
    - path: "test/unit/auth_persistence_test.dart"
      provides: "Scaffold for AUTH-02 session persistence tests"
      contains: "auth"
    - path: "integration_test/auth_test.dart"
      provides: "Scaffold for AUTH-01 full signup flow integration test"
      contains: "auth"
    - path: "integration_test/city_placement_test.dart"
      provides: "Scaffold for AUTH-04 trigger behavior integration test"
      contains: "city"
    - path: "integration_test/rls_test.dart"
      provides: "Scaffold for INFR-02 and INFR-03 RLS enforcement integration test"
      contains: "rls"
    - path: "test/helpers/mocks.dart"
      provides: "Shared mock classes for Supabase client, auth, and GoRouter"
      contains: "Mock"
  key_links:
    - from: "test/helpers/mocks.dart"
      to: "lib/core/supabase/supabase_provider.dart"
      via: "Mocks the Supabase client for unit tests"
      pattern: "MockSupabaseClient"
---

<objective>
Create the test infrastructure (Wave 0) required by all subsequent plans. This plan produces test scaffolds and helper files so that every task in Plans 01-03 can run `flutter test` as part of its verify block.

Purpose: The Nyquist validation contract requires `flutter test` to run after every task commit. Without test files existing, `flutter test` would either fail or have nothing to verify. Wave 0 creates the skeleton test files with pending/trivial tests that later plans will flesh out with real assertions as production code is written.
Output: Test directory structure with scaffold test files for all 6 phase requirements, shared mock helpers, and mocktail dev dependency.
</objective>

<execution_context>
@C:/Users/Bilal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bilal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure, mock helpers, and scaffold test files</name>
  <files>
    pubspec.yaml,
    test/helpers/mocks.dart,
    test/helpers/test_helpers.dart,
    test/unit/profile_validation_test.dart,
    test/unit/auth_persistence_test.dart,
    integration_test/auth_test.dart,
    integration_test/city_placement_test.dart,
    integration_test/rls_test.dart
  </files>
  <action>
    1. Add test dependencies (mocktail is needed for mocking Supabase client):
       ```bash
       flutter pub add --dev mocktail
       ```
       Also ensure `integration_test` is in dev_dependencies (Flutter SDK provides it):
       ```yaml
       # In pubspec.yaml dev_dependencies:
       integration_test:
         sdk: flutter
       ```

    2. Create `test/helpers/mocks.dart`:
       - Import `package:mocktail/mocktail.dart`
       - Create `MockSupabaseClient extends Mock implements SupabaseClient`
       - Create `MockGoRouter extends Mock implements GoRouter`
       - These will be used by unit tests in Plans 01-02 and 01-03

    3. Create `test/helpers/test_helpers.dart`:
       - Helper function `createTestApp(Widget child)` that wraps a widget with MaterialApp for widget testing
       - Helper function `createTestProviderScope(Widget child, {List<Override>? overrides})` that wraps with ProviderScope
       - These reduce boilerplate in individual test files

    4. Create `test/unit/profile_validation_test.dart` (AUTH-03 scaffold):
       - Import flutter_test
       - Create a test group 'Profile Validation' with placeholder tests:
         - `test('display name must be 3-20 characters', () { /* TODO: implement when profile screen is built */ });`
         - `test('display name must be alphanumeric + underscore', () { /* TODO: implement */ });`
         - `test('duplicate display name shows error', () { /* TODO: implement */ });`
       - Mark tests as pending using `skip: 'Waiting for production code in Plan 01-03'`

    5. Create `test/unit/auth_persistence_test.dart` (AUTH-02 scaffold):
       - Import flutter_test
       - Create a test group 'Auth Persistence' with placeholder tests:
         - `test('auth state provider emits session on auth change', skip: 'Waiting for auth provider in Plan 01-02');`
         - `test('current user provider returns user from session', skip: 'Waiting for auth provider in Plan 01-02');`

    6. Create `integration_test/auth_test.dart` (AUTH-01 scaffold):
       - Import flutter_test and integration_test
       - Create a test group 'Auth Integration' with placeholder:
         - `testWidgets('user can sign up with email and password', (tester) async { /* TODO: needs running Supabase */ }, skip: true);`

    7. Create `integration_test/city_placement_test.dart` (AUTH-04 scaffold):
       - Import flutter_test and integration_test
       - Create a test group 'City Placement' with placeholder:
         - `testWidgets('new user gets city auto-placed on island', (tester) async { /* TODO: needs running Supabase */ }, skip: true);`

    8. Create `integration_test/rls_test.dart` (INFR-02 + INFR-03 scaffold):
       - Import flutter_test and integration_test
       - Create a test group 'RLS Enforcement' with placeholder:
         - `testWidgets('client cannot INSERT into cities table', (tester) async { /* TODO: needs running Supabase */ }, skip: true);`
         - `testWidgets('client cannot INSERT into islands table', (tester) async { /* TODO: needs running Supabase */ }, skip: true);`

    9. Run `flutter test` to verify all scaffolds are recognized and skipped tests don't cause failures.
  </action>
  <verify>
    <automated>cd "C:/Users/Bilal/Desktop/Projects/ikariam" && flutter test --reporter compact 2>&1 | tail -10</automated>
  </verify>
  <done>Test directory structure exists (test/unit/, test/helpers/, integration_test/). All 5 scaffold test files created with skipped placeholder tests. mocktail dev dependency added. `flutter test` completes with zero failures (all tests skipped or trivially passing). Mock helpers ready for use by Plans 01-02 and 01-03.</done>
</task>

</tasks>

<verification>
- `flutter test --reporter compact` completes with exit code 0
- All scaffold test files exist in the correct directories
- mocktail is in pubspec.yaml dev_dependencies
- integration_test SDK dependency is in pubspec.yaml
</verification>

<success_criteria>
1. `flutter test` runs and exits cleanly (zero failures)
2. test/helpers/mocks.dart exists with MockSupabaseClient and MockGoRouter
3. All 5 requirement-mapped test scaffold files exist
4. Subsequent plans can include `flutter test` in their verify blocks without test infrastructure errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md`
</output>
