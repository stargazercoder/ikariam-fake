---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - lib/features/profile/screens/create_profile_screen.dart
  - lib/features/profile/widgets/avatar_picker.dart
  - lib/features/city/screens/city_screen.dart
  - lib/shared/widgets/avatar_widget.dart
autonomous: false
requirements:
  - AUTH-03

must_haves:
  truths:
    - "User who just signed up is redirected to profile creation screen (cannot bypass)"
    - "User can pick an avatar from 20 ancient Greek themed preset options"
    - "User can enter a display name (3-20 chars, alphanumeric + underscore)"
    - "Display name uniqueness is enforced — duplicate shows 'name taken' error"
    - "After completing profile, user lands on the city placeholder screen"
    - "User's city name is displayed on the city screen (auto-assigned by trigger)"
  artifacts:
    - path: "lib/features/profile/screens/create_profile_screen.dart"
      provides: "Profile creation form with avatar picker and display name input"
      min_lines: 80
    - path: "lib/features/profile/widgets/avatar_picker.dart"
      provides: "Grid of 20 selectable avatar options"
      min_lines: 30
    - path: "lib/features/city/screens/city_screen.dart"
      provides: "Placeholder city screen showing city name and basic info"
      min_lines: 30
    - path: "lib/shared/widgets/avatar_widget.dart"
      provides: "Reusable avatar display widget"
      min_lines: 15
  key_links:
    - from: "lib/features/profile/screens/create_profile_screen.dart"
      to: "lib/features/profile/data/profile_repository.dart"
      via: "Submit calls updateProfile"
      pattern: "updateProfile"
    - from: "lib/features/profile/screens/create_profile_screen.dart"
      to: "lib/core/constants/avatar_constants.dart"
      via: "Avatar list from constants"
      pattern: "AvatarConstants"
    - from: "lib/core/router/app_router.dart"
      to: "lib/features/profile/providers/profile_provider.dart"
      via: "Redirect checks profile completeness"
      pattern: "hasCompletedProfile"
    - from: "lib/features/city/screens/city_screen.dart"
      to: "supabase/migrations/20260311000002_create_cities.sql"
      via: "Fetches city data from cities table"
      pattern: "from.*cities"
---

<objective>
Profile creation screen with avatar picker and display name input, plus the placeholder city screen that users land on after completing their profile.

Purpose: AUTH-03 requires users to set a display name and avatar before entering the game. The profile creation screen is the mandatory gate between signup and gameplay. The city screen is the destination — it proves the entire auth-to-profile-to-city chain works end-to-end.
Output: Profile creation screen with avatar picker, display name validation, uniqueness error handling. City placeholder screen showing the auto-assigned city. Full auth flow verified from signup to city view.
</objective>

<execution_context>
@C:/Users/Bilal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bilal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Avatar widget, avatar picker, and profile creation screen</name>
  <files>
    lib/shared/widgets/avatar_widget.dart,
    lib/features/profile/widgets/avatar_picker.dart,
    lib/features/profile/screens/create_profile_screen.dart
  </files>
  <action>
    1. Create `lib/shared/widgets/avatar_widget.dart`:
       - A reusable `AvatarWidget` that displays an avatar given an `avatarId` (int 1-20)
       - Renders a colored circle (based on avatarId) with the first letter of the avatar label from AvatarConstants
       - Accepts optional `size` parameter (default 48)
       - Accepts optional `isSelected` parameter that adds a border/highlight ring
       - Uses the app theme colors (navy blue ring when selected, gold accent)

    2. Create `lib/features/profile/widgets/avatar_picker.dart`:
       - A widget that displays a GridView of 20 AvatarWidgets
       - 4 columns on desktop, 5 columns on mobile
       - Each avatar is tappable — calls `onAvatarSelected(int avatarId)` callback
       - Currently selected avatar has the `isSelected` highlight
       - Below each avatar circle, show the label (Zeus, Athena, etc.) in small text

    3. Create `lib/features/profile/screens/create_profile_screen.dart`:
       - ConsumerStatefulWidget
       - Title: "Create Your Profile" in app theme style
       - AvatarPicker at the top — defaults to avatar_id=1, user can tap to change
       - TextFormField for display name:
         - Validator: 3-20 characters, `RegExp(r'^[a-zA-Z0-9_]{3,20}$')`
         - Helper text: "3-20 characters, letters, numbers, and underscore"
         - On validation failure show inline error
       - ElevatedButton "Enter the Game"
         - On tap: call `ref.read(profileRepositoryProvider).updateProfile(userId: currentUser.id, displayName: name, avatarId: selectedAvatarId)`
         - Catch `DisplayNameTakenException` -> show SnackBar "That display name is already taken"
         - Catch generic errors -> show SnackBar with error message
         - On success: invalidate profileProvider (to re-fetch), then `context.go('/city')`
       - Use LoadingOverlay during submission
       - Style: centered layout, max-width 500px, Card with padding, clean Material 3
       - IMPORTANT: This screen must NOT have a back button to login — user is already authenticated, they must complete their profile
       - Add a small "Sign Out" text button at the bottom for users who want to switch accounts
  </action>
  <verify>
    <automated>cd "C:/Users/Bilal/Desktop/Projects/ikariam" && flutter analyze --no-fatal-infos && flutter test --reporter compact 2>&1 | tail -5</automated>
  </verify>
  <done>Avatar widget renders colored circle with label initial. Avatar picker shows 20 options in a grid with selection highlight. Profile creation screen has avatar picker, display name field with validation, submit button with error handling for duplicate names, and sign out option. flutter analyze and flutter test pass.</done>
</task>

<task type="auto">
  <name>Task 2: City placeholder screen and end-to-end flow wiring</name>
  <files>
    lib/features/city/screens/city_screen.dart,
    lib/features/city/data/city_repository.dart,
    lib/features/city/providers/city_provider.dart,
    lib/features/city/providers/city_provider.g.dart
  </files>
  <action>
    1. Create `lib/features/city/data/city_repository.dart`:
       - Class `CityRepository` with:
         - `Future<Map<String, dynamic>?> fetchPlayerCity(String ownerId)` — queries `supabase.from('cities').select('*, islands(*)').eq('owner_id', ownerId).maybeSingle()`
         - This fetches the city with its island data (join)
       - Create a Riverpod provider

    2. Create `lib/features/city/providers/city_provider.dart`:
       - An `@riverpod` AsyncNotifier that loads the current user's city on initialization
       - Exposes city name, island info (grid_x, grid_y, luxury_type)
       - Run `dart run build_runner build --delete-conflicting-outputs`

    3. Create `lib/features/city/screens/city_screen.dart`:
       - ConsumerWidget
       - Title bar: city name (from provider) — shows loading indicator while fetching
       - Body content (placeholder for Phase 2):
         - Show city name prominently
         - Show island info: "Island at (grid_x, grid_y) — Luxury: marble/crystal/sulfur"
         - Show player display name and avatar in an AppBar action or header
         - A centered text: "Your city awaits... Buildings coming in Phase 2"
       - AppBar with:
         - City name as title
         - Player avatar + name as trailing action
         - A "Sign Out" IconButton that calls auth signOut and GoRouter redirects to /login
       - Style: clean Material 3, use app theme
       - This is a placeholder — Phase 2 adds actual building content

    4. Run `dart run build_runner build --delete-conflicting-outputs`
    5. Run `flutter analyze --no-fatal-infos`
  </action>
  <verify>
    <automated>cd "C:/Users/Bilal/Desktop/Projects/ikariam" && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -3 && flutter analyze --no-fatal-infos && flutter test --reporter compact 2>&1 | tail -5</automated>
  </verify>
  <done>City repository fetches player's city with island join. City provider loads city data reactively. City screen shows city name, island location, luxury type, player info, and sign out. All Riverpod code-gen files generated. flutter analyze and flutter test pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 auth flow end-to-end</name>
  <action>Human verifies the complete signup-to-city flow works correctly in Chrome.</action>
  <what-built>Complete Phase 1 auth flow: signup -> profile creation -> city view, with session persistence and auto city placement</what-built>
  <how-to-verify>
    1. Start Supabase local: `supabase start` (if not already running)
    2. Reset database: `supabase db reset`
    3. In Supabase Studio (http://localhost:54323), go to Authentication > Settings and disable "Enable email confirmations" for easier dev testing
    4. Run the app: `flutter run -d chrome --dart-define=SUPABASE_URL=http://localhost:54321 --dart-define=SUPABASE_ANON_KEY=<your-anon-key>` (anon key is shown by `supabase status`)
    5. Verify: landing on /login screen (unauthenticated)
    6. Click "Sign Up" -> fill email + password -> submit
    7. Verify: redirected to /create-profile screen
    8. Pick an avatar, enter a display name (3-20 chars, alphanum + underscore) -> submit
    9. Verify: redirected to /city screen showing city name and island info
    10. Refresh the browser (F5) -> verify you're still on /city (session persists, AUTH-02)
    11. In Supabase Studio (http://localhost:54323):
        - Check profiles table has a row with your display_name and avatar_id
        - Check cities table has a row with a city name and island reference
        - Try running: `INSERT INTO public.cities (owner_id, island_id, slot_number, name) VALUES (gen_random_uuid(), (SELECT id FROM islands LIMIT 1), 99, 'test');` — it should FAIL with RLS violation (INFR-02)
    12. Click "Sign Out" -> verify redirect to /login
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Full signup flow works: signup -> create profile -> city view
- Session persists after browser refresh (AUTH-02)
- Profile gate works: authenticated user without display_name cannot reach /city
- Display name uniqueness enforced (try creating two users with same name)
- RLS prevents client-side INSERT/UPDATE/DELETE on cities and islands
- Avatar picker shows 20 options, selection is visually indicated
- City screen shows auto-assigned city name and island info
- Sign out redirects to login
- `flutter test --reporter compact` passes with no failures
</verification>

<success_criteria>
1. New user can sign up, create profile (avatar + display name), and see their city
2. Profile creation screen validates display name format (3-20 chars, alphanumeric + underscore)
3. Duplicate display name shows "name taken" error
4. City auto-assigned by trigger — city name and island visible on city screen
5. Browser refresh keeps user logged in on city screen
6. Unauthenticated access to /city redirects to /login
7. Authenticated user without profile redirects to /create-profile
8. `flutter test` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
