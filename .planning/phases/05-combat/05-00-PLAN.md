---
phase: 05-combat
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/unit/battle_models_test.dart
  - test/unit/combat_formula_test.dart
autonomous: true
requirements: [CMBT-01, CMBT-02, CMBT-03, CMBT-04, CMBT-05]

must_haves:
  truths:
    - "All CMBT requirements have at least one test stub targeting them"
    - "Test files compile and all stubs are skipped with descriptive messages"
  artifacts:
    - path: "test/unit/battle_models_test.dart"
      provides: "Skipped test stubs for Battle.fromJson, BattleTurn.fromJson, status checks, JSONB parsing"
    - path: "test/unit/combat_formula_test.dart"
      provides: "Skipped test stubs for combat formula, naval gate-keeper, Town Wall bonus"
  key_links: []
---

<objective>
Wave 0: Create test scaffold stubs for all Phase 5 combat requirements.

Purpose: Establish failing/skipped test expectations before any production code exists, following the project's established Wave 0 pattern (Phases 1-4).
Output: 2 test files with skipped stubs covering Battle model, BattleTurn model, combat formula logic, and naval gate-keeper behavior.
</objective>

<execution_context>
@C:/Users/Bilal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bilal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-combat/05-CONTEXT.md
@.planning/phases/05-combat/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test scaffolds for battle models and combat formula</name>
  <files>test/unit/battle_models_test.dart, test/unit/combat_formula_test.dart</files>
  <action>
Create two test files with skipped stubs following the established Wave 0 pattern (skip string messages for unit tests).

**test/unit/battle_models_test.dart** — stubs for:
- CMBT-01: Battle.fromJson parses all fields correctly (id, defenderCityId, attackerCityId, attackerId, defenderId, attackerUnits, defenderUnits, status, turnNumber, nextTurnAt, createdAt)
- CMBT-01: Battle.isActive returns true for 'active' status, false for 'attacker_won'/'defender_won'
- CMBT-02: BattleTurn.fromJson parses all fields including nullable naval/land casualties
- CMBT-03: BattleTurn.fromJson handles navalOutcome 'skipped' when no naval units
- CMBT-04: BattleTurn navalAttackerCasualties and landAttackerCasualties are Map<String, int>? (nullable)
- CMBT-05: No toJson/insert methods exist on models (read-only, server writes only)

Each stub: `test('description', () { /* TODO: implement in 05-02 */ }, skip: 'Stub — implements in 05-02');`

**test/unit/combat_formula_test.dart** — stubs for:
- CMBT-02: unitAttackStats and unitDefenseStats maps contain all 13 unit types
- CMBT-02: computeCasualties returns correct floor(enemy_attack / own_defense * own_units) clamped to [0, quantity]
- CMBT-03: navalUnitTypes contains exactly 5 types (cargo_ship, ram_ship, catapult_ship, mortar_ship, diving_boat)
- CMBT-03: Naval gate-keeper: if attacker naval wiped, battle should end
- CMBT-02: Town Wall bonus multiplies defender land defense by (1 + 0.05 * wall_level)

Each stub: `test('description', () { /* TODO: implement in 05-02 */ }, skip: 'Stub — implements in 05-02');`
  </action>
  <verify>
    <automated>cd C:/Users/Bilal/Desktop/Projects/ikariam && flutter test test/unit/battle_models_test.dart test/unit/combat_formula_test.dart</automated>
  </verify>
  <done>Both test files compile, all stubs are skipped with descriptive messages, flutter test reports 0 failures</done>
</task>

</tasks>

<verification>
flutter test test/unit/ — all existing + new tests pass (new ones skipped)
</verification>

<success_criteria>
- 2 new test files exist with skipped stubs
- Every CMBT requirement has at least one test stub
- flutter test passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-combat/05-00-SUMMARY.md`
</output>
