---
phase: 16-espionage-city-viewing
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/unit/espionage_test.dart
  - test/widget/enemy_city_view_test.dart
autonomous: true
requirements: [ESPY-01, ESPY-02]
must_haves:
  truths:
    - "test/unit/espionage_test.dart exists with stub tests for SpyReport.fromJson and BuildingCell readOnly"
    - "test/widget/enemy_city_view_test.dart exists with a smoke test stub for EnemyCityViewScreen"
  artifacts:
    - path: "test/unit/espionage_test.dart"
      provides: "Unit test stubs for espionage model and readOnly param"
      contains: "SpyReport.fromJson"
    - path: "test/widget/enemy_city_view_test.dart"
      provides: "Widget test stub for enemy city view screen"
      contains: "EnemyCityViewScreen"
  key_links: []
---

<objective>
Create Wave 0 test scaffold files required by the Nyquist validation strategy. These stub files must exist before Plans 16-01 and 16-02 execute, so their `<verify>` commands have valid test targets.

Purpose: Satisfies Nyquist rule — every `<automated>` verify command must reference an existing test file.
Output: Two test stub files with pending/skipped tests that will be filled in by Plans 16-01 and 16-02.
</objective>

<execution_context>
@C:/Users/Bilal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bilal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-espionage-city-viewing/16-CONTEXT.md
@.planning/phases/16-espionage-city-viewing/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files for espionage unit and widget tests</name>
  <files>
    test/unit/espionage_test.dart
    test/widget/enemy_city_view_test.dart
  </files>
  <read_first>
    test/unit/battle_models_test.dart
    test/widget/barracks_screen_test.dart
  </read_first>
  <action>
**1. Create `test/unit/espionage_test.dart`:**

Follow the pattern from `battle_models_test.dart`. Create a test file with:

```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('SpyReport.fromJson', () {
    test('parses valid JSONB report data with all fields', () {
      // Stub — will be implemented when spy_report.dart exists (Plan 16-01)
    }, skip: 'Wave 0 stub — awaiting Plan 16-01 implementation');

    test('handles missing optional fields with defaults', () {
    }, skip: 'Wave 0 stub — awaiting Plan 16-01 implementation');

    test('uses (v as num).toInt() for numeric JSONB fields', () {
    }, skip: 'Wave 0 stub — awaiting Plan 16-01 implementation');
  });

  group('BuildingCell readOnly', () {
    test('readOnly=true sets onTap to null', () {
    }, skip: 'Wave 0 stub — awaiting Plan 16-02 implementation');
  });
}
```

**2. Create `test/widget/enemy_city_view_test.dart`:**

Follow the pattern from `barracks_screen_test.dart`. Create a test file with:

```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('EnemyCityViewScreen', () {
    testWidgets('renders red AppBar with city name and owner name', (tester) async {
      // Stub — will be implemented when enemy_city_view_screen.dart exists (Plan 16-02)
    }, skip: 'Wave 0 stub — awaiting Plan 16-02 implementation');

    testWidgets('shows read-only notice banner', (tester) async {
    }, skip: 'Wave 0 stub — awaiting Plan 16-02 implementation');
  });
}
```

Both files must compile and pass `flutter test` (skipped tests count as passing).
  </action>
  <verify>
    <automated>cd C:/Users/Bilal/Desktop/Projects/ikariam && flutter test test/unit/espionage_test.dart && flutter test test/widget/enemy_city_view_test.dart</automated>
  </verify>
  <done>Both test files exist, compile, and pass flutter test (all tests skipped as stubs). Plans 16-01 and 16-02 can now reference these files in their verify commands.</done>
</task>

</tasks>

<verification>
- test/unit/espionage_test.dart exists and passes `flutter test`
- test/widget/enemy_city_view_test.dart exists and passes `flutter test`
- Both contain skipped stub tests matching VALIDATION.md requirements
</verification>

<success_criteria>
- Both test files pass `flutter test` without errors
- Stubs cover SpyReport.fromJson (ESPY-01), BuildingCell readOnly (ESPY-02), and EnemyCityViewScreen (ESPY-02)
</success_criteria>

<output>
After completion, create `.planning/phases/16-espionage-city-viewing/16-00-SUMMARY.md`
</output>
