---
phase: 03-world-map
plan: 02
type: execute
wave: 3
depends_on:
  - 03-01
files_modified:
  - lib/features/map/screens/world_map_screen.dart
  - lib/features/map/screens/island_screen.dart
  - lib/features/map/screens/city_grid_screen.dart
  - lib/features/map/constants/building_positions.dart
  - lib/features/city/screens/city_screen.dart
  - lib/core/router/app_router.dart
  - lib/features/auth/data/auth_repository.dart
  - test/unit/city_grid_test.dart
  - test/widget/world_map_smoke_test.dart
autonomous: false
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05

must_haves:
  truths:
    - "Player sees islands as colored squares on a pannable/zoomable 2D grid on the World Map tab"
    - "Tapping an island on the world map switches to the Island tab showing that island's detail"
    - "Island view shows 16-17 city slots with resource areas, occupied slots distinguished from empty"
    - "Tapping an owned city slot switches to the City tab"
    - "City tab shows buildings on a spatial 2D grid with tap-to-upgrade functionality preserved"
    - "kBuildingPositions maps all 14 BuildingType values to unique grid positions"
  artifacts:
    - path: "lib/features/map/screens/world_map_screen.dart"
      provides: "InteractiveViewer wrapping island grid"
      min_lines: 40
    - path: "lib/features/map/screens/island_screen.dart"
      provides: "Island detail with city slots and resource areas"
      min_lines: 50
    - path: "lib/features/map/screens/city_grid_screen.dart"
      provides: "Stack-based building grid wrapper for city view"
      min_lines: 40
    - path: "lib/features/map/constants/building_positions.dart"
      provides: "kBuildingPositions map for all 14 building types"
      exports: ["kBuildingPositions"]
    - path: "test/unit/city_grid_test.dart"
      provides: "Tests for building position completeness and uniqueness"
    - path: "test/widget/world_map_smoke_test.dart"
      provides: "Smoke test for WorldMapScreen rendering"
  key_links:
    - from: "lib/features/map/screens/world_map_screen.dart"
      to: "lib/features/map/providers/islands_provider.dart"
      via: "ref.watch(allIslandsProvider)"
      pattern: "allIslandsProvider"
    - from: "lib/features/map/screens/world_map_screen.dart"
      to: "lib/features/map/providers/islands_provider.dart"
      via: "ref.read(selectedIslandIdProvider.notifier)"
      pattern: "selectedIslandIdProvider"
    - from: "lib/features/map/screens/island_screen.dart"
      to: "lib/features/map/providers/island_detail_provider.dart"
      via: "ref.watch(islandDetailProvider(effectiveId))"
      pattern: "islandDetailProvider"
    - from: "lib/features/map/screens/city_grid_screen.dart"
      to: "lib/features/map/constants/building_positions.dart"
      via: "kBuildingPositions[building.buildingType]"
      pattern: "kBuildingPositions"
    - from: "lib/features/map/screens/city_grid_screen.dart"
      to: "lib/features/city/screens/building_upgrade_sheet.dart"
      via: "showBuildingUpgradeSheet on tap"
      pattern: "showBuildingUpgradeSheet"
---

<objective>
Three view screens (World Map, Island, City Grid) completing the visual navigation layer.

Purpose: Implement all three map views that let the player navigate between world, island, and city levels. This is the final plan for Phase 3 — after this, the player has a complete spatial navigation experience.
Output: WorldMapScreen, IslandScreen, CityGridScreen, building position constants, and human-verified end-to-end navigation.
</objective>

<execution_context>
@C:/Users/Bilal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bilal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-world-map/03-CONTEXT.md
@.planning/phases/03-world-map/03-RESEARCH.md
@.planning/phases/03-world-map/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 03-01 outputs — executor must use these exact types -->

From lib/features/map/models/island.dart:
```dart
class Island {
  final String id;
  final int gridX;
  final int gridY;
  final String luxuryType;
  final int maxCitySlots;
  factory Island.fromJson(Map<String, dynamic> json);
}
```

From lib/features/map/models/island_city_slot.dart:
```dart
class CitySlot {
  final int slotNumber;
  final String? cityName;
  final String? ownerId;
  bool get isOccupied;
  factory CitySlot.fromJson(Map<String, dynamic> json);
}

class IslandDetail {
  final Island island;
  final List<CitySlot> citySlots;
  int get cityCount;
}
```

From lib/features/map/providers/islands_provider.dart:
```dart
final allIslandsProvider = FutureProvider<List<Island>>(...)
final selectedIslandIdProvider = StateProvider<String?>(...)
```

From lib/features/map/providers/island_detail_provider.dart:
```dart
final islandDetailProvider = FutureProvider.family<IslandDetail, String>(...)
```

From lib/features/map/screens/main_shell_screen.dart:
```dart
class MainShellScreen extends StatelessWidget {
  const MainShellScreen({super.key, required this.navigationShell});
  final StatefulNavigationShell navigationShell;
}
```

From lib/core/router/app_router.dart:
```dart
// StatefulShellRoute.indexedStack with 3 branches:
//   /map   -> WorldMapScreen (placeholder from 03-01)
//   /island -> IslandScreen (placeholder from 03-01)
//   /city   -> CityScreen (existing)
```

From lib/features/city/screens/city_screen.dart:
```dart
class CityScreen extends ConsumerWidget { ... }
// _CityBody, _ResourcePanel, _ConstructionBanner, _BuildingsList, _BuildingRow
// showBuildingUpgradeSheet(context, building:, cityId:, currentResources:, activeConstruction:)
```

From lib/core/constants/building_constants.dart:
```dart
enum BuildingType {
  townHall, warehouse, barracks, shipyard, academy, embassy,
  tradingPort, townWall, hideout, tavern, sawmill, quarry,
  glassblower, sulfurPit;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Building positions, World Map, Island View, and City Grid screens</name>
  <files>lib/features/map/constants/building_positions.dart, lib/features/map/screens/world_map_screen.dart, lib/features/map/screens/island_screen.dart, lib/features/map/screens/city_grid_screen.dart, lib/features/city/screens/city_screen.dart, lib/core/router/app_router.dart, lib/features/auth/data/auth_repository.dart, test/unit/city_grid_test.dart, test/widget/world_map_smoke_test.dart</files>
  <behavior>
    - kBuildingPositions contains all 14 BuildingType values (no missing, no extra)
    - kBuildingPositions has no duplicate (row, col) positions
    - WorldMapScreen renders without throwing when given a mocked allIslandsProvider
  </behavior>
  <action>
**Building positions constant (lib/features/map/constants/building_positions.dart):**
- Define `const Map<BuildingType, ({int row, int col})> kBuildingPositions` with the layout from research:
  ```
  townHall: (2,2), warehouse: (0,0), barracks: (0,4), shipyard: (4,5),
  academy: (0,2), embassy: (2,0), tradingPort: (4,0), townWall: (2,5),
  hideout: (4,3), tavern: (4,1), sawmill: (1,1), quarry: (1,3),
  glassblower: (3,1), sulfurPit: (3,3)
  ```
- Grid is 6 columns x 5 rows. All 14 buildings fit with no overlapping cells.

**WorldMapScreen (lib/features/map/screens/world_map_screen.dart):**
- `class WorldMapScreen extends ConsumerWidget`
- Watches `allIslandsProvider` (FutureProvider)
- Shows loading spinner while fetching, error message on failure
- On data: `InteractiveViewer(constrained: false, boundaryMargin: EdgeInsets.all(80), minScale: 0.3, maxScale: 2.5)` wrapping a `SizedBox` sized for 5 columns x 2 rows (each cell ~120 logical pixels)
- Inside: `GridView.count(crossAxisCount: 5, physics: NeverScrollableScrollPhysics(), shrinkWrap: true)` with island cells
- Each island cell: a `GestureDetector` wrapping a colored `Container` with rounded corners
  - Color: navy/teal palette — vary shade by luxury type (marble=grey-blue, crystal=light-blue, sulfur=amber-tint) aligned with the existing navy/gold/white theme
  - Content: Text showing grid coordinates "(x,y)" and a small icon for luxury type
  - onTap: sets `ref.read(selectedIslandIdProvider.notifier).state = island.id`, then calls `context.go('/island')` to switch to Island tab
- Use `context.go('/island')` per research recommendation — do NOT use imperative `goBranch` deep in the tree. Set the selectedIslandIdProvider first, then navigate.

**IslandScreen (lib/features/map/screens/island_screen.dart):**
- `class IslandScreen extends ConsumerWidget`
- Reads `selectedIslandIdProvider` for the selected island ID
- Needs the player's own island ID as default: read from `cityProvider` (existing) which returns city data with `islands` join — extract `city['island_id']`
- `effectiveIslandId = selectedId ?? playerIslandId`
- If effectiveIslandId is null (still loading), show a loading spinner
- Watches `islandDetailProvider(effectiveIslandId)` — shows loading/error/data
- On data: a Column with:
  - Header: Island coordinates, luxury type, city count
  - `GridView.builder` with `crossAxisCount: 6` showing maxCitySlots + 2 items (slots + wood area + luxury area)
  - First maxCitySlots items: city slot cells
    - Empty slot: grey Container with slot number text
    - Occupied slot: colored Container (use the theme's primary color for player-owned, secondary for others) with city name text
    - Tapping a player-owned slot: `context.go('/city')` to switch to City tab
  - Last 2 items: resource area cells
    - Wood cell: green-tinted with forest icon
    - Luxury cell: colored per luxury type with matching icon (same icons as CityScreen's _luxuryIcon)
  - `physics: NeverScrollableScrollPhysics()` if inside a ScrollView, or normal physics if standalone

**CityGridScreen wrapper (lib/features/map/screens/city_grid_screen.dart):**
- `class CityGridScreen extends ConsumerWidget`
- This wraps the existing city data with a spatial grid layout
- Watches `cityProvider`, `buildingsStreamProvider(cityId)`, `resourcesStreamProvider(cityId)`, `constructionQueueProvider(cityId)` — same as current CityScreen
- Layout: Column with:
  - AppBar content (city name, avatar, sign-out) — move from CityScreen to the shell or keep in CityGridScreen
  - _ResourcePanel (reuse from city_screen.dart — extract to shared or import)
  - _ConstructionBanner (reuse)
  - The building grid: `SizedBox(width: 6 * cellSize, height: 5 * cellSize)` containing a `Stack` with:
    - Background: light grid lines (use `CustomPaint` or colored containers)
    - For each building from buildingsStreamProvider: `Positioned(left: pos.col * cellSize, top: pos.row * cellSize, width: cellSize, height: cellSize, child: _BuildingCell(...))`
  - `_BuildingCell`: tappable Container showing building icon + level number, colored by building type (production vs city), onTap calls `showBuildingUpgradeSheet`
  - cellSize: 56-64 logical pixels (choose based on fitting 6 columns comfortably)
- Center the Stack horizontally in the available width

**Update CityScreen / Router:**
- In `app_router.dart`: replace the placeholder WorldMapScreen and IslandScreen imports/builders with the real screen classes
- Per user decision: "Existing resource panel and construction banner remain above the grid" — keep _ResourcePanel and _ConstructionBanner. Replace _BuildingsList with the Stack grid.
- **Decision:** Create `city_grid_screen.dart` containing the `BuildingsGrid` widget and the `BuildingCell` widget (public, not underscore-prefixed). Import it into `city_screen.dart` and replace the `_BuildingsList` call with `BuildingsGrid`. This keeps file separation per user preference ("split into separate files").

**Logout cleanup (lib/features/auth/data/auth_repository.dart):**
- In the `signOut()` method, after calling `supabaseClient.auth.signOut()`, invalidate `selectedIslandIdProvider` to prevent stale island state per Pitfall 5 from research. This requires passing a `Ref` or `WidgetRef` — check how AuthRepository currently handles this. If AuthRepository does not have access to Ref, the invalidation can happen in the router redirect (when session becomes null, reset selectedIslandIdProvider).

**Tests — un-skip and implement:**

`test/unit/city_grid_test.dart`:
- Un-skip the 2 stubs from Wave 0
- Test 1: `kBuildingPositions.keys` contains exactly the 14 BuildingType.values (use `containsAll` and length check)
- Test 2: All (row, col) pairs in kBuildingPositions.values are unique (convert to Set of strings "row,col" and check Set length == 14)

`test/widget/world_map_smoke_test.dart`:
- Un-skip the widget test
- Create a simple smoke test: override `allIslandsProvider` with a list of 2-3 fake Island objects, pump `WorldMapScreen` wrapped in `ProviderScope` with overrides, verify no exceptions and find at least one island cell widget
  </action>
  <verify>
    <automated>cd C:/Users/Bilal/Desktop/Projects/ikariam && flutter test</automated>
  </verify>
  <done>All three screens render correctly. World map shows island grid with pan/zoom. Island view shows city slots and resource areas. City view shows building grid. All tests pass (unit + widget + existing).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of 3-tab navigation and all map views</name>
  <files>N/A</files>
  <action>
Human verifies the complete 3-tab navigation with World Map, Island View, and City Grid.

Steps to verify:
1. Run `supabase db reset` to apply the new 10-island seed
2. Run `flutter run -d chrome --dart-define-from-file=.env.local`
3. Log in (or sign up if needed)
4. World Map tab (should open by default after login): verify 10 colored squares visible in a grid layout, pan by dragging, zoom with scroll wheel, each square shows coordinates and a luxury type indicator
5. Tap an island — should switch to Island tab: verify grid of city slots (16-17 cells) plus 2 resource area cells, your city slot should be highlighted, resource areas should have distinct icons
6. Tap your city slot — should switch to City tab: verify buildings displayed on a spatial grid (not a flat list), resource panel and construction banner still visible, tap a building to open upgrade sheet
7. Tab switching: tap each tab in the bottom nav, each view should retain its state
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>All 3 views render correctly, navigation between tabs works, economy features (resources, upgrades) remain functional in the city grid view</done>
</task>

</tasks>

<verification>
- `flutter test` full suite green
- All 3 screens render and navigate correctly
- Existing economy functionality (resources, buildings, upgrades) unbroken
- Pan and zoom work on world map
- Cross-tab navigation (world -> island -> city) works
</verification>

<success_criteria>
- Player opens game to world map with 10 islands on a 2D grid (MAP-01, MAP-05)
- Each island shows city slots and resource areas on island view (MAP-02, MAP-03)
- City view displays buildings on a spatial grid with tap-to-upgrade (MAP-04)
- All navigation flows between tabs work correctly
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-world-map/03-02-SUMMARY.md`
</output>
