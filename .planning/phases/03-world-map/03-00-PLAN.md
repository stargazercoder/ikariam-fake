---
phase: 03-world-map
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/map_models_test.dart
  - test/unit/city_grid_test.dart
  - test/widget/world_map_smoke_test.dart
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05

must_haves:
  truths:
    - "All Phase 3 requirements have skipped test stubs before any production code is written"
    - "flutter test runs with zero failures (all new tests are skipped)"
  artifacts:
    - path: "test/unit/map_models_test.dart"
      provides: "Skipped stubs for Island model parsing, IslandDetail aggregation"
      contains: "skip"
    - path: "test/unit/city_grid_test.dart"
      provides: "Skipped stubs for building position map completeness"
      contains: "skip"
    - path: "test/widget/world_map_smoke_test.dart"
      provides: "Skipped stub for WorldMapScreen render smoke test"
      contains: "skip"
  key_links: []
---

<objective>
Wave 0 test scaffolds for Phase 3 World Map requirements.

Purpose: Establish skipped test stubs for every MAP requirement before any production code exists, following the same pattern used in Phase 1 (01-00) and Phase 2 (02-00).
Output: 3 test files with skipped stubs covering MAP-01 through MAP-05.
</objective>

<execution_context>
@C:/Users/Bilal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bilal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-world-map/03-CONTEXT.md
@.planning/phases/03-world-map/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test scaffolds for MAP requirements</name>
  <files>test/unit/map_models_test.dart, test/unit/city_grid_test.dart, test/widget/world_map_smoke_test.dart</files>
  <action>
Create three test files with skipped stubs. Follow the established pattern from Phase 1/2: unit tests use `skip: 'descriptive message'` (string), widget tests use `skip: true` (boolean).

**test/unit/map_models_test.dart:**
- `group('Island model')` with:
  - `test('parses grid_x and grid_y from Supabase JSON', skip: 'TODO: 03-01')` — MAP-01
  - `test('exposes max_city_slots and luxury_type', skip: 'TODO: 03-01')` — MAP-02
- `group('IslandDetail')` with:
  - `test('aggregates cities by slot_number correctly', skip: 'TODO: 03-01')` — MAP-03

**test/unit/city_grid_test.dart:**
- `group('Building positions')` with:
  - `test('kBuildingPositions contains all 14 BuildingType values', skip: 'TODO: 03-02')` — MAP-04
  - `test('kBuildingPositions has no duplicate row/col positions', skip: 'TODO: 03-02')` — MAP-04

**test/widget/world_map_smoke_test.dart:**
- `testWidgets('WorldMapScreen renders without error', skip: true)` — MAP-05
  - Body can be empty or contain a `// TODO: 03-02` comment

Import `package:flutter_test/flutter_test.dart` in all files. Import `package:ikariam/core/constants/building_constants.dart` in city_grid_test.dart (will compile because BuildingType already exists).
  </action>
  <verify>
    <automated>cd C:/Users/Bilal/Desktop/Projects/ikariam && flutter test test/unit/map_models_test.dart test/unit/city_grid_test.dart test/widget/world_map_smoke_test.dart</automated>
  </verify>
  <done>3 test files exist, all tests are skipped, flutter test reports 0 failures</done>
</task>

</tasks>

<verification>
- `flutter test` full suite passes with zero failures
- New test files contain skipped stubs for all 5 MAP requirements
</verification>

<success_criteria>
- 3 new test files committed
- Every MAP requirement (MAP-01 through MAP-05) has at least one skipped test stub
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/03-world-map/03-00-SUMMARY.md`
</output>
