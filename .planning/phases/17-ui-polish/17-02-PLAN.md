---
phase: 17-ui-polish
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - lib/features/map/screens/island_screen.dart
  - lib/features/map/screens/world_map_screen.dart
  - lib/features/map/providers/islands_provider.dart
  - lib/features/military/screens/barracks_screen.dart
  - lib/features/military/screens/shipyard_screen.dart
  - lib/features/military/screens/dispatch_screen.dart
  - lib/features/battles/screens/battle_detail_screen.dart
autonomous: true
requirements: [UIPL-02, UIPL-03]
nyquist_compliant: false  # Visual-only phase: styling/border/icon changes have no testable behavior beyond flutter analyze

must_haves:
  truths:
    - "Island screen city slots show green border for own cities, red for enemy, grey for empty"
    - "World map island cells show green border if player has city on island, red if enemy-only, grey if empty"
    - "Barracks screen unit rows use CircleAvatar with unit-type-specific icon and color"
    - "Shipyard screen unit rows use CircleAvatar with unit-type-specific icon and color"
    - "Dispatch screen unit rows use CircleAvatar with unit-type-specific icon and color"
    - "Battle detail screen unit rows use CircleAvatar with unit-type-specific icon and color"
    - "All ownership colors come from OwnershipColors class (no hardcoded green/red)"
  artifacts:
    - path: "lib/features/map/screens/island_screen.dart"
      provides: "Ownership-colored borders on city slots"
      contains: "OwnershipColors"
    - path: "lib/features/map/screens/world_map_screen.dart"
      provides: "Ownership-colored borders on island cells"
      contains: "OwnershipColors"
    - path: "lib/features/map/providers/islands_provider.dart"
      provides: "islandOwnershipProvider for world map ownership data"
      contains: "islandOwnershipProvider"
    - path: "lib/features/military/screens/barracks_screen.dart"
      provides: "CircleAvatar unit type icons in roster"
      contains: "unitTypeIcons"
    - path: "lib/features/military/screens/shipyard_screen.dart"
      provides: "CircleAvatar unit type icons in roster"
      contains: "unitTypeIcons"
    - path: "lib/features/military/screens/dispatch_screen.dart"
      provides: "CircleAvatar unit type icons in dispatch rows"
      contains: "unitTypeIcons"
    - path: "lib/features/battles/screens/battle_detail_screen.dart"
      provides: "CircleAvatar unit type icons in battle unit composition"
      contains: "unitTypeIcons"
  key_links:
    - from: "lib/features/map/screens/island_screen.dart"
      to: "lib/core/constants/ownership_colors.dart"
      via: "import and use in _CitySlotCell border color"
      pattern: "import.*ownership_colors"
    - from: "lib/features/map/screens/world_map_screen.dart"
      to: "lib/features/map/providers/islands_provider.dart"
      via: "ref.watch(islandOwnershipProvider)"
      pattern: "islandOwnershipProvider"
    - from: "lib/features/military/screens/barracks_screen.dart"
      to: "lib/core/constants/unit_constants.dart"
      via: "CircleAvatar using unitTypeColors and unitTypeIcons"
      pattern: "unitTypeIcons"
---

<objective>
Apply ownership color borders to map screens and unit type CircleAvatar icons to all military screens.

Purpose: Completes UIPL-02 (ownership color coding on maps) and UIPL-03 (unit type color coding on military screens). Uses constants created in Plan 01.
Output: Updated island screen, world map screen, and 4 military screen files with visual polish.
</objective>

<execution_context>
@C:/Users/Bilal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bilal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-polish/17-CONTEXT.md
@.planning/phases/17-ui-polish/17-RESEARCH.md
@.planning/phases/17-ui-polish/17-UI-SPEC.md
@.planning/phases/17-ui-polish/17-01-SUMMARY.md

<interfaces>
<!-- Constants created in Plan 01 -->

From lib/core/constants/ownership_colors.dart (created in Plan 01):
```dart
class OwnershipColors {
  OwnershipColors._();
  static const Color own   = Color(0xFF43A047); // green
  static const Color enemy = Color(0xFFE53935); // red
  static const Color empty = Color(0xFFBDBDBD); // grey
}
```

From lib/core/constants/unit_constants.dart (extended in Plan 01):
```dart
const Map<UnitType, IconData> unitTypeIcons = {
  UnitType.hoplite: Icons.shield, UnitType.phalanx: Icons.view_column,
  UnitType.archer: Icons.gps_fixed, UnitType.cavalry: Icons.speed,
  UnitType.catapult: Icons.launch, UnitType.mortar: Icons.expand_less,
  UnitType.medic: Icons.local_hospital, UnitType.cook: Icons.restaurant,
  UnitType.cargoShip: Icons.directions_boat, UnitType.ramShip: Icons.arrow_forward,
  UnitType.catapultShip: Icons.thunderstorm, UnitType.mortarShip: Icons.wb_sunny,
  UnitType.divingBoat: Icons.water,
};

const Map<UnitType, Color> unitTypeColors = { ... }; // existing 13 entries
UnitType unitTypeFromDbName(String dbName); // existing parser
```

From lib/features/map/screens/island_screen.dart:
```dart
// _CitySlotCell (line 462-505):
// - has _isPlayerOwned bool
// - uses theme.colorScheme.primary/secondary for color
// - Container with BoxDecoration, Border.all, BorderRadius.circular(6)
// - slot?.isOccupied check at line 438
```

From lib/features/map/screens/world_map_screen.dart:
```dart
// _IslandCell receives Island? — has NO ownership data
// _IslandGrid builds from allIslandsProvider (List<Island>)
// _islandColor() returns color based on luxuryType only
// border: Border.all(color: Colors.white.withAlpha(60), width: 1)
```

From lib/features/map/providers/islands_provider.dart:
```dart
final allIslandsProvider = FutureProvider<List<Island>>((ref) async { ... });
final selectedIslandIdProvider = NotifierProvider<SelectedIslandNotifier, String?>(...);
final playerIslandIdProvider = Provider<String?>((ref) { ... });
```

From lib/features/military/screens/barracks_screen.dart:
```dart
// Line 256: Icons.shield (training card)
// Line 455: leading: const Icon(Icons.shield) (roster ListTile)
```

From lib/features/military/screens/shipyard_screen.dart:
```dart
// Line 246: Icons.sailing (training card)
// Line 445: leading: const Icon(Icons.sailing) (roster ListTile)
```

From lib/features/military/screens/dispatch_screen.dart:
```dart
// Line 302: isNaval ? Icons.sailing : Icons.shield (dispatch row)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ownership color borders on island screen and world map</name>
  <files>lib/features/map/screens/island_screen.dart, lib/features/map/screens/world_map_screen.dart, lib/features/map/providers/islands_provider.dart</files>
  <read_first>
    - lib/features/map/screens/island_screen.dart
    - lib/features/map/screens/world_map_screen.dart
    - lib/features/map/providers/islands_provider.dart
    - lib/features/map/data/map_repository.dart
    - lib/core/constants/ownership_colors.dart
    - .planning/phases/17-ui-polish/17-CONTEXT.md
  </read_first>
  <action>
**Island Screen (island_screen.dart):**

1. Add import: `import '../../../core/constants/ownership_colors.dart';`

2. In `_CitySlotCell.build()` — the occupied slot section (around line 462):
   - Replace the color derivation logic:
     ```dart
     // OLD:
     // final color = _isPlayerOwned
     //     ? theme.colorScheme.primary
     //     : theme.colorScheme.secondary;

     // NEW:
     Color _slotBorderColor() {
       if (slot == null || !slot!.isOccupied) return OwnershipColors.empty;
       if (_isPlayerOwned) return OwnershipColors.own;
       return OwnershipColors.enemy;
     }
     final color = _slotBorderColor();
     ```

   - Update the Container decoration (around line 469-476) for BOTH empty and occupied slots:
     ```dart
     decoration: BoxDecoration(
       color: color.withAlpha(40),
       borderRadius: BorderRadius.circular(8),  // changed from 6 to 8 per CONTEXT.md
       border: Border.all(color: color, width: 2),
     ),
     ```

   - Also update the EMPTY slot decoration (around line 443-447) to use OwnershipColors.empty:
     ```dart
     decoration: BoxDecoration(
       color: OwnershipColors.empty.withAlpha(40),
       borderRadius: BorderRadius.circular(8),
       border: Border.all(color: OwnershipColors.empty, width: 2),
     ),
     ```

**World Map — Provider (islands_provider.dart):**

3. Add a new `islandOwnershipProvider` to `lib/features/map/providers/islands_provider.dart`:
   ```dart
   import 'package:supabase_flutter/supabase_flutter.dart';

   /// Returns ownership status per island: 'own', 'enemy', or absent (empty).
   /// Own city takes priority — if player has ANY city on the island, it's 'own'.
   final islandOwnershipProvider = FutureProvider<Map<String, String>>((ref) async {
     final currentUserId = Supabase.instance.client.auth.currentUser?.id;
     if (currentUserId == null) return {};

     final rows = await Supabase.instance.client
         .from('cities')
         .select('island_id, owner_id');

     final result = <String, String>{};
     for (final row in rows as List<dynamic>) {
       final islandId = row['island_id'] as String;
       final ownerId = row['owner_id'] as String;
       if (ownerId == currentUserId) {
         result[islandId] = 'own'; // own takes priority
       } else if (!result.containsKey(islandId) || result[islandId] != 'own') {
         result[islandId] = 'enemy';
       }
     }
     return result;
   });
   ```

**World Map Screen (world_map_screen.dart):**

4. Add imports:
   ```dart
   import '../../../core/constants/ownership_colors.dart';
   ```

5. In `_IslandGrid`, change to `ConsumerWidget` (it's already a `ConsumerWidget` — confirm). Add `ref.watch(islandOwnershipProvider)` and pass ownership data to `_IslandCell`:
   - In `_IslandGrid.build()`, add:
     ```dart
     final ownershipAsync = ref.watch(islandOwnershipProvider);
     final ownership = ownershipAsync.valueOrNull ?? <String, String>{};
     ```
   - Pass ownership status to each `_IslandCell`:
     ```dart
     _IslandCell(
       island: islandByPos['$col,$row'],
       gridX: col,
       gridY: row,
       ownershipStatus: island != null ? (ownership[island.id] ?? 'empty') : 'empty',
       onTap: ...,
     ),
     ```
   - But since `island` is looked up by position, use:
     ```dart
     final cellIsland = islandByPos['$col,$row'];
     final status = cellIsland != null ? (ownership[cellIsland.id] ?? 'empty') : 'empty';
     ```

6. Update `_IslandCell` to accept and use `ownershipStatus`:
   - Add `final String ownershipStatus;` field and constructor parameter
   - Update the Container border for populated islands (around line 183-186):
     ```dart
     Color _ownershipBorderColor() {
       switch (ownershipStatus) {
         case 'own': return OwnershipColors.own;
         case 'enemy': return OwnershipColors.enemy;
         default: return OwnershipColors.empty;
       }
     }

     // In Container decoration:
     border: Border.all(color: _ownershipBorderColor(), width: 2),
     ```
   - Keep the existing `_islandColor()` background fill based on luxury type — only the border changes
  </action>
  <verify>
    <automated>flutter analyze lib/features/map/screens/island_screen.dart lib/features/map/screens/world_map_screen.dart lib/features/map/providers/islands_provider.dart</automated>
  </verify>
  <acceptance_criteria>
    - island_screen.dart contains `import` of `ownership_colors.dart`
    - island_screen.dart contains `OwnershipColors.own` (not `theme.colorScheme.primary`)
    - island_screen.dart contains `OwnershipColors.enemy` (not `theme.colorScheme.secondary`)
    - island_screen.dart contains `OwnershipColors.empty`
    - island_screen.dart contains `BorderRadius.circular(8)` (updated from 6)
    - island_screen.dart contains `Border.all(color: color, width: 2)` or equivalent using OwnershipColors
    - islands_provider.dart contains `final islandOwnershipProvider = FutureProvider`
    - islands_provider.dart contains `'own'` and `'enemy'` string literals
    - islands_provider.dart contains `from('cities').select('island_id, owner_id')`
    - world_map_screen.dart contains `import` of `ownership_colors.dart`
    - world_map_screen.dart contains `islandOwnershipProvider`
    - world_map_screen.dart `_IslandCell` has `ownershipStatus` parameter
    - world_map_screen.dart contains `OwnershipColors.own`
    - `flutter analyze` reports no errors on all 3 files
  </acceptance_criteria>
  <done>Island screen city slots use OwnershipColors for border (green own, red enemy, grey empty); world map island cells show ownership-based 2px colored border via new islandOwnershipProvider</done>
</task>

<task type="auto">
  <name>Task 2: CircleAvatar unit type icons on all military screens</name>
  <files>lib/features/military/screens/barracks_screen.dart, lib/features/military/screens/shipyard_screen.dart, lib/features/military/screens/dispatch_screen.dart, lib/features/battles/screens/battle_detail_screen.dart</files>
  <read_first>
    - lib/features/military/screens/barracks_screen.dart
    - lib/features/military/screens/shipyard_screen.dart
    - lib/features/military/screens/dispatch_screen.dart
    - lib/features/battles/screens/battle_detail_screen.dart
    - lib/core/constants/unit_constants.dart
    - .planning/phases/17-ui-polish/17-UI-SPEC.md
  </read_first>
  <action>
All 4 military screens need the same pattern: replace generic `Icons.shield` / `Icons.sailing` with a `CircleAvatar` that uses `unitTypeColors[type]` as background and `unitTypeIcons[type]` as the icon.

**Helper function** — add this as a top-level function or inline in each file (or better, add to `unit_constants.dart` as a widget helper is bad practice — just inline the pattern):

Pattern to apply at each replacement point:
```dart
// Inline CircleAvatar for unit type
CircleAvatar(
  radius: 16,
  backgroundColor: unitTypeColors[unitType] ?? Colors.grey,
  child: Icon(
    unitTypeIcons[unitType] ?? Icons.help_outline,
    color: Colors.white,
    size: 16,
  ),
)
```

Where `unitType` is the parsed `UnitType` enum value from `unitTypeFromDbName(dbName)`.

**barracks_screen.dart:**

1. At line 256 (training card icon area) — replace `Icons.shield` with the CircleAvatar pattern. The unit type is available from the `_landUnits` list iteration. Use:
   ```dart
   CircleAvatar(
     radius: 16,
     backgroundColor: unitTypeColors[unit],
     child: Icon(unitTypeIcons[unit] ?? Icons.help_outline, color: Colors.white, size: 16),
   )
   ```
   where `unit` is the `UnitType` from the loop variable.

2. At line 455 (roster `ListTile` leading) — replace `const Icon(Icons.shield)` with:
   ```dart
   leading: Builder(builder: (context) {
     UnitType? type;
     try { type = unitTypeFromDbName(u.unitType); } catch (_) {}
     if (type == null) return const Icon(Icons.help_outline);
     return CircleAvatar(
       radius: 16,
       backgroundColor: unitTypeColors[type],
       child: Icon(unitTypeIcons[type] ?? Icons.help_outline, color: Colors.white, size: 16),
     );
   }),
   ```
   where `u` is the `CityUnit` in the roster list.

**shipyard_screen.dart:**

3. At line 246 (training card icon area) — same pattern as barracks, replace `Icons.sailing` with CircleAvatar using the naval `UnitType` from the loop.

4. At line 445 (roster `ListTile` leading) — same pattern as barracks line 455, replace `const Icon(Icons.sailing)` with CircleAvatar using `unitTypeFromDbName(u.unitType)`.

**dispatch_screen.dart:**

5. At line 302 (dispatch row icon) — replace `isNaval ? Icons.sailing : Icons.shield` with CircleAvatar:
   ```dart
   // The unit type string is available as the row's unit type key.
   // Parse it to UnitType and use CircleAvatar.
   CircleAvatar(
     radius: 16,
     backgroundColor: unitTypeColors[unitType] ?? Colors.grey,
     child: Icon(unitTypeIcons[unitType] ?? Icons.help_outline, color: Colors.white, size: 16),
   )
   ```
   Read the dispatch_screen.dart carefully to find exactly how the unit type string is available in the `_UnitDispatchRow` widget and parse it.

**battle_detail_screen.dart:**

6. In the unit composition section (around line 418-442) where unit entries are mapped to `Row` widgets, add a small CircleAvatar leading each unit row:
   ```dart
   (e) => Padding(
     padding: const EdgeInsets.symmetric(vertical: 1),
     child: Row(
       mainAxisAlignment: MainAxisAlignment.spaceBetween,
       children: [
         // NEW: unit type icon
         Builder(builder: (_) {
           UnitType? type;
           try { type = unitTypeFromDbName(e.key); } catch (_) {}
           if (type == null) return const SizedBox(width: 24);
           return Padding(
             padding: const EdgeInsets.only(right: 6),
             child: CircleAvatar(
               radius: 10,
               backgroundColor: unitTypeColors[type],
               child: Icon(unitTypeIcons[type] ?? Icons.help_outline, color: Colors.white, size: 10),
             ),
           );
         }),
         Expanded(
           child: Text(_displayName(e.key), style: theme.textTheme.bodySmall, overflow: TextOverflow.ellipsis),
         ),
         Text('${e.value}', style: theme.textTheme.bodySmall?.copyWith(fontWeight: FontWeight.bold)),
       ],
     ),
   ),
   ```
   Use smaller radius (10) since battle detail rows are compact.

Ensure all 4 files import `unit_constants.dart` (barracks and shipyard already do; confirm dispatch and battle_detail do).
  </action>
  <verify>
    <automated>flutter analyze lib/features/military/screens/barracks_screen.dart lib/features/military/screens/shipyard_screen.dart lib/features/military/screens/dispatch_screen.dart lib/features/battles/screens/battle_detail_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - barracks_screen.dart contains `unitTypeIcons` (used in CircleAvatar)
    - barracks_screen.dart contains `CircleAvatar` (replacing plain Icon)
    - barracks_screen.dart does NOT contain `leading: const Icon(Icons.shield)` (old pattern removed)
    - shipyard_screen.dart contains `unitTypeIcons`
    - shipyard_screen.dart contains `CircleAvatar`
    - shipyard_screen.dart does NOT contain `leading: const Icon(Icons.sailing)` (old pattern removed)
    - dispatch_screen.dart contains `unitTypeIcons`
    - dispatch_screen.dart contains `CircleAvatar`
    - dispatch_screen.dart does NOT contain `isNaval ? Icons.sailing : Icons.shield` as leading icon pattern
    - battle_detail_screen.dart contains `unitTypeIcons`
    - battle_detail_screen.dart contains `CircleAvatar`
    - `flutter analyze` reports no errors on all 4 files
  </acceptance_criteria>
  <done>All 4 military screens display unit-type-specific CircleAvatar icons with colors from unitTypeColors and icons from unitTypeIcons; generic shield/sailing icons fully replaced</done>
</task>

</tasks>

<verification>
- `flutter analyze` passes with no errors on all 7 modified files
- Island screen city slots render with green (own), red (enemy), grey (empty) 2px borders
- World map island cells render with ownership-based 2px colored borders
- All military screens (barracks, shipyard, dispatch, battle detail) show CircleAvatar with per-unit-type icon and color
- No hardcoded ownership colors — all come from OwnershipColors constants
- No generic Icons.shield / Icons.sailing remaining in military screen leading widgets
</verification>

<success_criteria>
- UIPL-02 complete: Island screen and world map show ownership color borders (green/red/grey)
- UIPL-03 complete: All military screens use CircleAvatar with unitTypeIcons and unitTypeColors
- Color coding is consistent across all screens that display units or city markers
- No flutter analyze errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-polish/17-02-SUMMARY.md`
</output>
