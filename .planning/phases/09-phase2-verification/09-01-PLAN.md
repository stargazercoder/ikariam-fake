---
phase: 09-phase2-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-core-economy/02-VERIFICATION.md
autonomous: true
requirements:
  - RSRC-01
  - RSRC-02
  - RSRC-03
  - RSRC-04
  - BLDG-01
  - BLDG-02
  - BLDG-03
  - BLDG-04
  - BLDG-05
must_haves:
  truths:
    - "02-VERIFICATION.md exists in .planning/phases/02-core-economy/"
    - "All 9 requirement IDs (RSRC-01 through RSRC-04, BLDG-01 through BLDG-05) have rows in Requirements Coverage table"
    - "Each requirement has evidence citing specific file paths and line ranges"
    - "Observable Truths table has at least 9 rows covering all requirement behaviors"
    - "Human Verification Required section lists runtime-only truths (pg_cron firing, Realtime updates, UI rejection)"
    - "Key Link Verification table connects Flutter providers to DB tables and Edge Functions"
  artifacts:
    - path: ".planning/phases/02-core-economy/02-VERIFICATION.md"
      provides: "Phase 2 verification report for all RSRC and BLDG requirements"
      contains: "RSRC-01"
  key_links:
    - from: "02-VERIFICATION.md"
      to: "REQUIREMENTS.md"
      via: "requirement ID references"
      pattern: "(RSRC|BLDG)-0[1-5]"
---

<objective>
Create the missing Phase 2 (Core Economy) VERIFICATION.md document that formally verifies all 9 RSRC and BLDG requirements against the existing codebase.

Purpose: Phase 2 was fully implemented but its verification document was never created — this is the only gap remaining in the v1.0 milestone audit. All code exists; this is purely a documentation/audit task.
Output: `.planning/phases/02-core-economy/02-VERIFICATION.md`
</objective>

<execution_context>
@C:/Users/Bilal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bilal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-phase2-verification/09-RESEARCH.md

# Format references — read these to match the exact VERIFICATION.md structure
@.planning/phases/01-foundation/01-VERIFICATION.md
@.planning/phases/08-bug-fixes-timer-guards/08-VERIFICATION.md

# Phase 2 SUMMARYs — what was actually built
@.planning/phases/02-core-economy/02-01-SUMMARY.md
@.planning/phases/02-core-economy/02-02-SUMMARY.md
@.planning/phases/02-core-economy/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Phase 2 source files and collect evidence</name>
  <files>
    supabase/migrations/20260311000004_create_city_resources.sql
    supabase/migrations/20260311000005_create_city_buildings.sql
    supabase/migrations/20260311000006_create_construction_queue.sql
    supabase/migrations/20260311000007_on_city_created_trigger.sql
    supabase/migrations/20260311000008_resource_production_functions.sql
    supabase/migrations/20260311000009_construction_functions.sql
    supabase/migrations/20260311000010_pg_cron_jobs.sql
    supabase/functions/upgrade-building/index.ts
    lib/core/constants/building_constants.dart
    lib/core/constants/resource_constants.dart
    lib/features/city/data/buildings_repository.dart
    lib/features/city/providers/resources_provider.dart
    lib/features/city/providers/buildings_provider.dart
    lib/features/city/providers/construction_provider.dart
    test/unit/building_formulas_test.dart
  </files>
  <action>
    Read all 15 source files listed above and record exact line numbers for each piece of evidence. This is read-only inspection — no code changes.

    For each requirement, record:
    - RSRC-01: city_resources CHECK constraint listing 5 types, on_city_created trigger seeding all 5, ResourceType enum in resource_constants.dart, resourcesStreamProvider wiring
    - RSRC-02: cron.schedule('resource-tick', '*/5 * * * *', ...) in pg_cron_jobs.sql — note exact line
    - RSRC-03: `r.workers * r.prod_level * 1.0` formula in process_resource_tick() — note exact line. Note research_bonus is hard-coded 1.0 for v1.
    - RSRC-04: LEAST(amount + production, 500.0 * POWER(1.5, warehouse_level)) in process_resource_tick() — note exact line. Also warehouseCapacity() in resource_constants.dart.
    - BLDG-01: CHECK constraint with 14 types (10 required + 4 production) in city_buildings migration, BuildingType enum in building_constants.dart. Note: 4 extra production buildings are implementation detail per STATE.md decision — BLDG-01 is satisfied.
    - BLDG-02: calcUpgradeCost() with COST_GROWTH_FACTOR=1.5 in upgrade-building/index.ts, upgradeCost() with costGrowthFactor=1.5 in building_constants.dart. Also building_formulas_test.dart tests.
    - BLDG-03: calcUpgradeDurationMinutes() with TIME_GROWTH_FACTOR=1.2 in upgrade-building/index.ts, upgradeDurationMinutes() with timeGrowthFactor=1.2 in building_constants.dart. Also building_formulas_test.dart tests. Note base times reduced to 1 min for testing — formula shape is correct per Pitfall 2.
    - BLDG-04: UNIQUE(city_id) in construction_queue migration + 409 queue-busy check in upgrade-building/index.ts + constructionQueueProvider in construction_provider.dart
    - BLDG-05: complete_building_upgrades() WHERE finish_at <= NOW() in construction_functions migration + construction-tick cron job in pg_cron_jobs.sql

    Also read the Phase 2 SUMMARYs (02-01, 02-02, 02-03) to understand plan-level artifact expectations.
    Also read 01-VERIFICATION.md and 08-VERIFICATION.md to match the exact document format (frontmatter, section headers, table columns).
  </action>
  <verify>
    <automated>echo "Read-only audit task — verify all 15 files exist:" && ls supabase/migrations/20260311000004_create_city_resources.sql supabase/migrations/20260311000005_create_city_buildings.sql supabase/migrations/20260311000006_create_construction_queue.sql supabase/migrations/20260311000007_on_city_created_trigger.sql supabase/migrations/20260311000008_resource_production_functions.sql supabase/migrations/20260311000009_construction_functions.sql supabase/migrations/20260311000010_pg_cron_jobs.sql supabase/functions/upgrade-building/index.ts lib/core/constants/building_constants.dart lib/core/constants/resource_constants.dart lib/features/city/data/buildings_repository.dart lib/features/city/providers/resources_provider.dart lib/features/city/providers/buildings_provider.dart lib/features/city/providers/construction_provider.dart test/unit/building_formulas_test.dart 2>&1</automated>
  </verify>
  <done>All 15 source files read and evidence collected with exact line numbers for each of the 9 requirements</done>
</task>

<task type="auto">
  <name>Task 2: Write 02-VERIFICATION.md</name>
  <files>.planning/phases/02-core-economy/02-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/02-core-economy/02-VERIFICATION.md` following the exact format from 01-VERIFICATION.md and 08-VERIFICATION.md.

    **Frontmatter:**
    - phase: 02-core-economy
    - verified: 2026-03-12
    - status: human_needed (because pg_cron runtime, Realtime UI updates, and second-upgrade UI rejection need live stack)
    - score: N/N must-haves verified (count from Observable Truths table)
    - human_verification: list 3-4 items that need a running Supabase stack (see below)

    **Observable Truths table** — derive from the 9 requirements plus wiring truths. Each row has: #, Truth, Status (VERIFIED or ? NEEDS HUMAN), Evidence (file:line citations). Expected truths:

    1. Cities have 5 resource types (wood, marble, crystal, sulfur, gold) — VERIFIED (migration + enum)
    2. pg_cron job 'resource-tick' registered with */5 * * * * schedule — VERIFIED (static from migration)
    3. resource-tick fires and resources increase every 5 minutes — ? NEEDS HUMAN (runtime)
    4. Production formula is workers x building_level x research_bonus — VERIFIED (migration function body)
    5. Resources capped at warehouse capacity via LEAST() — VERIFIED (migration function body)
    6. City supports 10+ building types (10 required + 4 production) — VERIFIED (CHECK constraint + enum)
    7. Upgrade cost formula is base_cost x 1.5^level — VERIFIED (Edge Function + Dart + unit tests)
    8. Upgrade duration formula is base_time x 1.2^level — VERIFIED (Edge Function + Dart + unit tests)
    9. Only one construction allowed per city (UNIQUE constraint + 409 check) — VERIFIED (static); ? NEEDS HUMAN (UI rejection test)
    10. Construction completion detected by pg_cron finish_at <= NOW() check — VERIFIED (static); ? NEEDS HUMAN (cron fires)
    11. Flutter resource display updates via Supabase Realtime stream — ? NEEDS HUMAN (runtime)
    12. Flutter building list updates after upgrade completion — ? NEEDS HUMAN (runtime)

    **Required Artifacts section** — group by plan (02-00, 02-01, 02-02, 02-03). List each file with Expected, Status (EXISTS/MISSING), and Details. Use the 02-*-SUMMARY.md files to know what each plan produced.

    **Key Link Verification table** — critical wiring connections:
    - resourcesStreamProvider -> city_resources table (via Supabase .stream())
    - buildingsStreamProvider -> city_buildings table (via Supabase .stream())
    - constructionQueueProvider -> construction_queue table (via Supabase .stream())
    - BuildingsRepository.upgradeBuilding -> upgrade-building Edge Function (via functions.invoke)
    - process_resource_tick() -> city_resources UPDATE (via SQL)
    - complete_building_upgrades() -> city_buildings UPDATE + construction_queue DELETE (via SQL)

    **Requirements Coverage table** — 9 rows, one per requirement ID. Columns: Requirement, Source Plan(s), Description, Status, Evidence.

    **Anti-Patterns Found section** — note the non-atomic resource deduction in upgrade-building (documented exception for v1), and the BASE_COSTS/BASE_TIMES duplication between TS and Dart (sync comment pattern).

    **Human Verification Required section** — list items that need a live Supabase stack:
    1. pg_cron resource-tick fires every 5 minutes and resources actually increase
    2. Supabase Realtime pushes resource/building updates to Flutter without page refresh
    3. Second upgrade request shows rejection in UI (409 response rendered as error)
    4. Construction countdown timer displays correctly and building level increments after completion

    **Gaps Summary section** — should state "No implementation gaps found. All 9 requirements have code evidence. Human verification items are runtime confirmation only."

    IMPORTANT: Use exact file:line evidence collected in Task 1. Do NOT invent line numbers — use the actual values from the audit.
  </action>
  <verify>
    <automated>grep -c "RSRC-0[1-4]\|BLDG-0[1-5]" .planning/phases/02-core-economy/02-VERIFICATION.md</automated>
  </verify>
  <done>02-VERIFICATION.md exists with all 9 requirement IDs referenced, Observable Truths table populated, Key Links documented, Human Verification section present, and format matches 01-VERIFICATION.md/08-VERIFICATION.md</done>
</task>

</tasks>

<verification>
1. File exists: `ls .planning/phases/02-core-economy/02-VERIFICATION.md`
2. All 9 requirements referenced: `grep -c "RSRC-0[1-4]\|BLDG-0[1-5]" .planning/phases/02-core-economy/02-VERIFICATION.md` returns >= 18 (each ID appears in multiple sections)
3. Has required sections: `grep -c "Observable Truths\|Required Artifacts\|Key Link\|Requirements Coverage\|Human Verification\|Gaps Summary" .planning/phases/02-core-economy/02-VERIFICATION.md` returns 6
4. Frontmatter valid: `head -6 .planning/phases/02-core-economy/02-VERIFICATION.md` shows phase, verified, status, score fields
</verification>

<success_criteria>
- 02-VERIFICATION.md exists in `.planning/phases/02-core-economy/` with correct frontmatter
- All 9 requirement IDs (RSRC-01 through RSRC-04, BLDG-01 through BLDG-05) have entries in Requirements Coverage table
- Each requirement has file:line evidence citations (not vague references)
- Human Verification Required section lists 3-4 runtime-only items
- Document format matches existing 01-VERIFICATION.md and 08-VERIFICATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/09-phase2-verification/09-01-SUMMARY.md`
</output>
