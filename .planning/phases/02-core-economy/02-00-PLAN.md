---
phase: 02-core-economy
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/unit/building_time_test.dart
  - integration_test/building_upgrade_test.dart
  - integration_test/resource_production_test.dart
autonomous: true
requirements: [RSRC-01, RSRC-03, RSRC-04, BLDG-02, BLDG-03, BLDG-04]

must_haves:
  truths:
    - "flutter test runs and exits with zero test failures (scaffolds are skipped pending production code)"
    - "Wave 0 test stub files exist for all VALIDATION.md requirements before Wave 1 begins"
  artifacts:
    - path: "test/unit/building_time_test.dart"
      provides: "Scaffold for BLDG-03: finish_at duration formula verification"
      contains: "test("
    - path: "integration_test/building_upgrade_test.dart"
      provides: "Scaffold for BLDG-02 (cost formula) and BLDG-04 (single queue enforcement)"
      contains: "testWidgets("
    - path: "integration_test/resource_production_test.dart"
      provides: "Scaffold for RSRC-01 (5 resource types), RSRC-03 (production formula), RSRC-04 (warehouse cap)"
      contains: "testWidgets("
  key_links:
    - from: "test/unit/building_time_test.dart"
      to: "lib/core/constants/building_constants.dart"
      via: "Tests import building constants for formula verification"
      pattern: "building_constants"
---

<objective>
Create Wave 0 test infrastructure for Phase 2. These are stub/scaffold test files required by the VALIDATION.md Nyquist contract so that every subsequent plan can run `flutter test` in its verify block without missing test file errors.

Purpose: VALIDATION.md specifies 3 Wave 0 test files as prerequisites. Without them, Plans 01-03 cannot reference these files in their verify commands. Wave 0 creates the skeleton test files with skipped/pending tests that later plans will flesh out with real assertions.

Output: 3 test scaffold files with skipped placeholder tests. `flutter test` completes with zero failures.
</objective>

<execution_context>
@C:/Users/Bilal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bilal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-economy/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test scaffolds for Phase 2 requirements</name>
  <files>
    test/unit/building_time_test.dart,
    integration_test/building_upgrade_test.dart,
    integration_test/resource_production_test.dart
  </files>
  <action>
    Create 3 test scaffold files matching the VALIDATION.md Wave 0 requirements. Follow the same pattern established in Phase 1 Plan 01-00: skipped placeholder tests with clear TODO comments indicating which plan will implement them.

    **test/unit/building_time_test.dart (BLDG-03):**
    - Import `package:flutter_test/flutter_test.dart`
    - Test group: 'Building Upgrade Time Formula'
    - Skipped tests:
      - `test('upgradeDurationMinutes returns base_time for level 0', skip: 'Waiting for building_constants.dart in Plan 02-02')`
      - `test('upgradeDurationMinutes applies 1.2^level growth factor', skip: 'Waiting for building_constants.dart in Plan 02-02')`
      - `test('upgradeDurationMinutes returns ceil of fractional result', skip: 'Waiting for building_constants.dart in Plan 02-02')`
    - NOTE: Plan 02-02 Task 1 (which has tdd="true") will replace these skipped stubs with real RED-GREEN tests for building_constants.dart. The stubs here simply ensure `flutter test` can reference this file without errors.

    **integration_test/building_upgrade_test.dart (BLDG-02, BLDG-04):**
    - Import `package:flutter_test/flutter_test.dart` and `package:integration_test/integration_test.dart`
    - `IntegrationTestWidgetsFlutterBinding.ensureInitialized();` at top
    - Test group: 'Building Upgrade Integration'
    - Skipped tests:
      - `testWidgets('upgrade-building Edge Function deducts resources and inserts queue entry', (tester) async {}, skip: true)` — covers BLDG-02
      - `testWidgets('second upgrade request while queue active returns 409', (tester) async {}, skip: true)` — covers BLDG-04
      - `testWidgets('upgrade with insufficient resources returns 400', (tester) async {}, skip: true)` — covers BLDG-02 edge case

    **integration_test/resource_production_test.dart (RSRC-01, RSRC-03, RSRC-04):**
    - Import `package:flutter_test/flutter_test.dart` and `package:integration_test/integration_test.dart`
    - `IntegrationTestWidgetsFlutterBinding.ensureInitialized();` at top
    - Test group: 'Resource Production Integration'
    - Skipped tests:
      - `testWidgets('new city has 5 resource rows with correct initial amounts', (tester) async {}, skip: true)` — covers RSRC-01
      - `testWidgets('process_resource_tick increases resources based on workers x level formula', (tester) async {}, skip: true)` — covers RSRC-03
      - `testWidgets('resource amounts do not exceed warehouse capacity after tick', (tester) async {}, skip: true)` — covers RSRC-04

    Run `flutter test` to verify all scaffolds are recognized and skipped tests cause no failures.
  </action>
  <verify>
    <automated>cd /c/Users/Bilal/Desktop/Projects/ikariam && flutter test test/unit/building_time_test.dart --reporter compact && flutter test --reporter compact 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 3 test scaffold files exist at the paths specified in VALIDATION.md Wave 0
    - `flutter test` completes with zero failures (all new tests are skipped)
    - Plans 01-03 can now reference these test files in their verify commands
  </done>
</task>

</tasks>

<verification>
```bash
# All test files exist
ls test/unit/building_time_test.dart integration_test/building_upgrade_test.dart integration_test/resource_production_test.dart

# flutter test completes cleanly
cd /c/Users/Bilal/Desktop/Projects/ikariam && flutter test --reporter compact
```
</verification>

<success_criteria>
- `flutter test` runs and exits with zero failures
- All 3 VALIDATION.md Wave 0 test files exist with skipped placeholder tests
- Subsequent plans can include `flutter test` in their verify blocks
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-economy/02-00-SUMMARY.md`
</output>
