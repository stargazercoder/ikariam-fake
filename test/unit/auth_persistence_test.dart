// Scaffold for AUTH-02: Session persistence tests.
//
// These tests will be implemented in Plan 01-02 once the Riverpod auth
// state provider is built.

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Auth Persistence', () {
    test(
      'auth state provider emits session on auth change',
      () {
        // TODO: implement when auth provider is built in Plan 01-02
      },
      skip: 'Waiting for auth provider in Plan 01-02',
    );

    test(
      'current user provider returns user from session',
      () {
        // TODO: implement when auth provider is built in Plan 01-02
      },
      skip: 'Waiting for auth provider in Plan 01-02',
    );
  });
}
